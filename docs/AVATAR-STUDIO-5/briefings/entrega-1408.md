# Onda 1408 — Fase 0 P0: LABORATÓRIO 3D — looks registry, overlays de QA, calibração, famílias de material, skin tint UBC por metadado, thumbs ACES+env (MEGA_BRIEFING_01 Parte 8 §1756–§1767/§2001–§2010; Parte 7 §1507–§1518/§1625; §105/§107–§109/§141–§146; §695–§697; decisões #160/#161/#165a/#165c)

> Entrega 2026-08-19. Mapa: claude/41. Três flags novas, todas **OFF** (mudam render/UI visível → ligam após validação visual do Jhony): `as6.looks`, `as6.material_v2`, `as6.qa_visual` (filhas de `as5.palco3d`). Com as flags OFF o render é **byte-idêntico** (regressão visual 111/111 `identico/expected`, inclusive os 16 frames 3D). Goldens g01–g16 intactos. Suíte completa em background antes da entrega.

## Entregue

| # | Item | Arquivo | §§ |
|---|---|---|---|
| 1 | **Lighting Registry** `Looks3d.ts`: `CANONICO` (= valores de `montar()`), looks `estudio@1` (byte-idêntico), `soft/cool/neon` (aliases exatos de quente/fria/neon), **`portrait`** (fill alto, key frontal suave, rim branco, exp 1.05, câmera retrato) e **`dramatic`** (PoC adaptada) — novos só com `as6.looks`; aliases dos 3 vocabulários (legado shell, 2D `LUZES_PALCO`, PoC); `etiquetaLook()` (`look@versao` p/ metadados de captura) | `src/services/Looks3d.ts` | §48–§51, §1756–§1767, §1960, §2001–§2006 |
| 2 | `Renderizador3d.aplicarLook(id)` (key/fill/ambiente/env/rim do look/exposição base; não toca fundo/câmera/aro/partículas), exposição efetiva = base do look × slider do usuário, ambiente do usuário vence o look; `lookAtivo()` | `src/services/Renderizador3d.ts` | §1765–§1767, §1873 |
| 3 | Palco3d: com `as6.looks` a luz vem do registry (`lookDaLuzLegada`), chips **Retrato/Dramática** (`p3d-look-*`), tone-mapping modes viram **dev-only** (`as5.hud3d`) — §1872; `Cenas3d.LUZES_3D` só cresce (`portrait`, `dramatic`) | `src/shell/Palco3d.tsx`, `src/services/Cenas3d.ts` | §1872, §1999 |
| 4 | **Overlays de QA** `definirOverlay('nenhum'\|'clay'\|'normals'\|'wireframe'\|'silhueta'\|'grayscale')` via `scene.overrideMaterial` (zero lib) com restauração exata; **cena de calibração** `definirLaboratorio(on)` (fundo cinza 18 %, look estudio, color checker de 6 esferas gerado em código: branco/preto/cinza 18 %/metal/pele ref/emissivo); UI dev no grupo Cinema (`p3d-qa`, `p3d-overlay-*`, `p3d-lab`) sob `as6.qa_visual` | `Renderizador3d.ts`, `Palco3d.tsx` | §105, §107–§109, §141–§146 |
| 5 | **Snapshot de métricas** `snapshotMetricas()` (look@versao, exposição efetiva, tone mapping, env, fov, posição da câmera, luzes [tipo/cor/intensidade/pos], texturas/programas/geometrias/drawCalls/triângulos, overlay, laboratório) + handle dev `window.__avst3d` (só `as5.hud3d`) p/ HUD/testes/QaStudio | `Renderizador3d.ts`, `Palco3d.tsx` | §2010, §1686–§1690 |
| 6 | **Material Family Registry** `FamiliasMaterial.ts`: 34 famílias versionadas (skin 3 tiers, eyes/teeth naoTingir, hair ×4, cotton/denim/wool/knit/satin/silk/technical, leather ×3, rubber/plastic ×3, metal_brushed/polished, gold/silver/bronze naoTingir, armor, glass ×3, crystal, hologram, energy, emissive), `ALBEDO_METAL` PBR-safe, `corPbrSegura()`; `Materiais3d.aplicarFamilias()` (aplica só materiais com família DECLARADA; guarda `pbrOriginal`; tier) — nenhum manifest declara família ainda (1421, após before/after) | `src/services/FamiliasMaterial.ts`, `src/services/Materiais3d.ts` | §1509–§1518, §1597–§1599, §1631–§1635 |
| 7 | **Skin tint das bases UBC por metadado (#165a)**: `Materiais3d.marcarMateriaisPorManifest()` (canal §73 / `naoTingir` / família via manifest v2 `materiais`), `aplicarPipelineCores` respeita `naoTingir`; `Renderizador3d.definirMateriaisV2()` marca antes do assembler e no pipeline; `Assembler3d` passo "pele" reconhece pele por metadado (sem copy — o tint multiplicativo fica no pipeline, sem dupla aplicação); manifests `base_superhero_m/f` declaram `materiais: { MI_Superhero_*: {canal: pele}, MI_Eyes: {naoTingir}, MI_Hair_*: {canal: cabelo} }`; Palco3d liga por `as6.material_v2` | `Materiais3d.ts`, `Renderizador3d.ts`, `Assembler3d.ts`, `Palco3d.tsx`, manifests UBC | §549–§550, §695–§697, §1516–§1518 |
| 8 | `gerar-thumbs-3d.mjs` = palco: **ACES (exp 1.0) + RoomEnvironment 0.55** (look estudio@1) e grava `look: 'estudio@1'` no manifest ao gerar (#165c). **Thumbs publicadas NÃO regeneradas** (★ commit próprio após ok do Jhony — imagens do catálogo mudam) | `scripts/avatar/assets3d/gerar-thumbs-3d.mjs` | §1625, §1880, §2001–§2003 |
| 9 | `auditar-materiais.mjs` → `evidencias/materiais-3d.json` (83 materiais em 34 assets; 35 com normal map; 48 sem textura; 83 OPAQUE; suspeitos: 5 peles sem textura nos Quaternius legados; UBC pele/olhos/cabelo têm normal map e metallicRoughness map) | `scripts/avatar/assets3d/auditar-materiais.mjs` | §1507, §1749.1 |
| 10 | Teste `looks3d.mjs`: A node (estudio == CANONICO == montar(); aliases; soft/cool/neon == definirLuz; 34 famílias válidas; naoTingir; corPbrSegura; manifests UBC + schema v2; auditoria determinística) · B navegador (luzes/exposição/env/tone **idênticos** OFF vs ON = contrato estudio@1; chips novos; Retrato = +rim e exp 1.05; tone dev-only sem hud3d; overlay clay/normals mudam e "Sem overlay"/calibração **restauram byte a byte**; `as6.material_v2` ON: `MI_Superhero_Male` recebe o canal pele (#5f3d23) e `MI_Eyes` fica branco; OFF: pele intocada); `pesos-esperados.json` Renderizador3d 42→52 KB (justificado) | `scripts/avatar/testes/looks3d.mjs` | — |

## Achados/decisões operacionais

- O harness ignora `dshow.avatar.config.v1` (API mock devolve `config: null` com sucesso) — testes que precisam de cores personalizadas usam o painel "Cores e propriedades" (como `materiais3d.mjs`); registrado para a rota de QA (1410).
- A PoC R3F (`poc3d/Cena3D.LUZES`) continua com o rig próprio (aliases mapeados; migração p/ registry com fallback na 1420).
- Pele dos Quaternius legados é cor chapada (sem textura) — família `skin` só compensa em parte; UBC tem mapas.
- Next (1409): homologação/LOD/perf/telemetria; golden-iluminacao com os looks entra na matriz de regressão quando `as6.looks` for validada.

## Próxima: 1409 — homologação (renders N ângulos), auditoria/republicação de LODs (★), clipping QA, corpo-benchmark (clay/silhueta via overlays), perf por asset + budgets, telemetria de asset, validador de cabelo/roupa.
