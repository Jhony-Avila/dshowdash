# Onda 1409 — Fase 0 P0: HOMOLOGAÇÃO, LODs, perf por asset, telemetria de asset, corpo-benchmark (MEGA_BRIEFING_01 §96, §148–§151, §1597–§1599, §1631–§1635, §1749–§1755, §2625–§2650, §2783–§2804, §2946, §2968–§2972; decisão #165b)

> Entrega 2026-08-19. Mapa: claude/41. Uma flag nova, **ON** (dado/observabilidade, #156 tooling): `as6.telemetria_assets` (filha de `as5.palco3d`) — callback ausente/OFF = zero eventos; nada muda no render. Tudo o mais é tooling/validação (node) sem efeito em runtime. Regressão visual 111/111, goldens intactos. Suíte completa **147/147 verde** antes da entrega (2ª rodada; 1ª rodada 145/147 → homologacao.mjs: regra de LOD virou nota; looks3d [B2]: aquecimento antes do frame base).

## Entregue

| # | Item | Arquivo | §§ |
|---|---|---|---|
| 1 | **Renders de homologação** `gerar-renders-homologacao.mjs <pasta> [--angulos] [--modos] [--lods]`: headless fora da UI (look estudio@1, ACES, câmera por bbox), modos normal/clay/silhueta/wireframe × 4 ângulos × 3 LODs → PNGs + `metricas.json` (IoU da silhueta LODn×LOD0 por ângulo, triângulos/drawCalls/texturas reais do renderer, **landmarks do rig**: altura/largura/profundidade, head/neck/ombros/pelvis/mão/pé, larguraOmbros, cabecasNaAltura, maoAteIndicador). UBC: IoU lod2 0,993–0,997 | `scripts/avatar/assets3d/gerar-renders-homologacao.mjs` | §96, §148–§151, §2637–§2650 |
| 2 | **Auditoria de LODs** `auditar-lods.mjs [--silhueta]` → `evidencias/lods-3d.json`: classe por geometria (`identicos`/`lod1=lod0`/`lod2=lod1`/`decrescente`), bytes redundantes, razão de redução. **Achado: 27/34 idênticos + 7 lod1=lod0 (UBC), 22,4 MB redundantes de 52,3 MB; decimação real: nenhum** (#165b) | `scripts/avatar/assets3d/auditar-lods.mjs`, `docs/AVATAR-STUDIO-5/evidencias/lods-3d.json` | §2625–§2636, #165b |
| 3 | **Validador — regra de LOD**: LODs sem decimação = **nota** informativa em production/legacy (não muda o status — contrato "UBC sai limpo" da 611+ preservado; nunca reprovação retroativa), **erro em premium/hero** (`excecoes.lod` declarada → nota); premium decrescente aquém de 50 %/20 % = nota (§2630). `relatorioDeValidacao` imprime `Nota:`. `medidas.classeLod`. **Validador de cabelo/roupa**: ≤ 3 materiais; BLEND exige `alpha: 'blend'` no manifest; MASK/BLEND sem baseColorTexture = alpha sem mapa; `medidas.alpha` (aviso production / erro premium+). `texturasDoGlb()` exportado | `scripts/avatar/assets3d/validar-asset.mjs` | §1631–§1635, §2625–§2636 |
| 4 | **Perf por asset** `medir-perf-asset.mjs [<pasta>|--json|--render]` + **`budgets.json` v1** (classes da tabela PERFORMANCE-BUDGETS §3: `tipo` ou `tipo:perfClasse`; `fatorLod` tri 50 %/20 %, `fatorVramLod` ¼/¹⁄₁₆; regressão histórica +40 % bytes / +50 % VRAM vs JSON anterior) → `evidencias/perf-assets.json` (determinístico). Política: acima do teto = aviso (production) / erro (premium+). **Achado: 0/34 dentro dos budgets** (bases Quaternius 7–11 materiais; UBC 7 texturas 2048², VRAM est. 107 MB; cabelos/roupas UBC 2048² na classe de 1024², LOD2 > 20 %) — nada reprova; republicação ★ | `scripts/avatar/assets3d/medir-perf-asset.mjs`, `budgets.json`, `evidencias/perf-assets.json` | §2783–§2804, §2946 |
| 5 | **Telemetria de asset** (`as6.telemetria_assets`, ON): `OpcoesRenderizador3d.aoEventoAsset` + `EventoAsset3d` (`asset_carregou` slug/lod/ms · `asset_falhou` erro ≤ 80 chars · `lod_transicao` lodAnterior→lod · `fallback_ativado` `standin_lod2`/`rig_incompativel` · `parte_carregou/parte_falhou`), sem URL; `Palco3d` aplica **rate limit ≤ 6/slug/min** e publica `avst:<evento>`; `TelemetriaDev` bloco **"Assets 3D"** (`tlm-assets`: carregou + média ms, lod_transicao, fallback, falhou + últimas falhas) | `src/services/Renderizador3d.ts`, `src/shell/Palco3d.tsx`, `src/shell/TelemetriaDev.tsx`, `nucleo/flags.ts` | §2804, §2968–§2972 |
| 6 | **Corpo-benchmark** `corpo-benchmark.mjs [--so <id>]`: bases → renders clay/silhueta (front/profile) + landmarks vs **faixas por estilo** (realista/estilizado/cartoon/criatura/robo; `manifest.estilo` ou heurística do id) → `evidencias/corpo-benchmark.json`. Calibração inicial 1409: 8/8 dentro (UBC 8,2–8,7 cabeças, ombros/altura 0,17–0,23; Quaternius 5,1–6,8 cabeças; androide 4,79 m nativo — §487 escala, informativo) | `scripts/avatar/assets3d/corpo-benchmark.mjs`, `evidencias/corpo-benchmark.json` | §1749–§1755, ART-BIBLE §6 |
| 7 | `gerar-thumbs-3d.mjs --tipo <tipo>` (regeneração por tipo, ★ commit próprio) | `scripts/avatar/assets3d/gerar-thumbs-3d.mjs` | §1880 |
| 8 | Testes: `bundle-assets.mjs` (A bundle sem assets embutidos/base64 > 8 KB, dist/assets só CSS+workers · B budgets íntegros, perf determinístico, perf-assets.json coincide, UBC 14318 tri/7 tex/2048 · C production avisa / premium erra / classes / regressão histórica · D validador LOD + cabelo/roupa em fixtures · E auditar-lods determinístico + lods-3d.json coincide) · `homologacao-3d.mjs` (A 8 renders UBC, IoU ≥ 0,92, landmarks, benchmark UBC na faixa, corpo-benchmark.json coincide · B navegador: `asset_carregou` com slug/lod/ms sem URL, `lod_transicao` ao trocar tier, rate limit 6/20, TelemetriaDev `tlm-assets`, OFF = zero eventos); `pesos-esperados.json` TelemetriaDev 7→9 KB (justificado) | `scripts/avatar/testes/{bundle-assets,homologacao-3d}.mjs`, `rodar-todos.mjs` (147) | — |
| 9 | Docs: PERFORMANCE-BUDGETS §3/§7 (política, medição 1409, telemetria), ASSET-PIPELINE §1/§7/§10 | `docs/AVATAR-STUDIO-5/*.md` | §3044 |

## Decisões operacionais (registro #45)

- **#167** Budgets por asset são **avisos** em tudo que já está publicado (`production/legacy/prototype`) e **erro** só em `premium/hero` — o pacote premium nasce dentro do orçamento; o legado não é reprovado retroativamente (§2804). Os 34 assets atuais ficam fora do budget (0/34) e isso é um **achado registrado**, não um bloqueio.
- **#168** Faixas do corpo-benchmark são **calibração inicial** medida no rig (distância entre bones, não silhueta) — mais largas que o cânone de desenho; refinar após validação visual (ART-BIBLE §6). `androide` mantém 4,79 m nativos (escala §487 informativa) sob a classe `robo`.
- **#169** Telemetria de asset nasce **ON** (#156: tooling/dado), com rate limit no shell e payload sem URL — o Renderizador3d só expõe o callback; destino/flag são do Palco3d.
- Itens do mapa 1409 **adiados para 1410** (rota de QA/CLI, onde ganham UI): clipping QA semiautomático (bbox/raycast), teste em movimento (3 frames UAL), `corpo-deformacao.mjs` (poses A–H), thumbs "costas", health score no `index.json`. Registrado no mapa.

## Precisa do Jhony (não bloqueia)

- ★ Republicar LODs com decimação real (27 idênticos + 7 lod1=lod0; 22,4 MB) e texturas 1024² em cabelos/roupas UBC → hashes/manifests mudam (commit próprio).
- ★ Regenerar thumbs com ACES+env (`--tipo` por tipo) — imagens do catálogo mudam.
- Aceite de `budgets.json` v1 como política (PERFORMANCE-BUDGETS "precisa do Jhony" #4).

## Próxima: 1410 — rota de QA/CLI (`as6.qa_route`): `cli.mjs publish --dry-run`, gates por nível (§2748), ficha de QA, health score, clipping QA, corpo-deformação, ingestão segura.
