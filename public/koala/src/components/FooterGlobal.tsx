// R3 — Rodapé global do Koala (todas as telas). VAZIO por ora (o dono colocará ícones/infos depois).
// Slot estrutural pronto: a faixa (.k-footer) vem do Shell; aqui só o conteúdo futuro.
export function FooterGlobal() {
  return <div className="k-footer-inner" aria-hidden="true" />;
}
